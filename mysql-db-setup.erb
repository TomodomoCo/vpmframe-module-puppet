# set up database
mysqldb { "<%= db_name %>":
        user => "<%= db_user %>",
        password => "<%= db_password %>",
        host => "<%= db_host %>",
        grant_to => "<%= db_grant_to %>",
}

